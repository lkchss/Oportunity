## Backlog
- [x] BUG FIXED (2026-06-10, commit 1d9071d): no-LSAT scholarship inflation — lsat=160 placeholder hit the splitter merit path. Now GPA-only merit + GPA-only percentile fit when LSAT is None. appalachian: None 83.0→62.9 (real-150 = 66.5). Snapshot diff: only p14/p15 (the two no-LSAT seeds) changed, lower-rank reshuffles only, top-5 stable. Regression test added (all lsat_75<=154 schools).
- [x] Audit follow-ups DONE (2026-06-10): claude_client.py deleted + google-generativeai dropped + no-LLM rule in CLAUDE.md (commit 05e1314); rounds 3-6 committed (9515bc3); mvp consolidation committed (b920619).
- [ ] Audit follow-ups remaining (low): decide fate of legacy law/main.py Streamlit UI (frozen pre-round-3); clamp 4 scholarship_pct values >1.0 in build_schools (dayton/iu-mckinney/mitchell-hamline/penn-state-dickinson, fallback-path only); law-only requirements file for Render build speed  → uses: none
### Law additions — non-LLM, audit 2026-06-10 (3 ABA datasets already on disk, unwired)
- [x] Wire 3 ABA quality reports (2026-06-10, commit 9e3de06): law/data/build_quality.py merges Attrition + Faculty Resources + Curricular Offerings into law_schools.json (196/196). New fields: attrition_1l_pct (range 0-21%, 6 schools >15%; flagged "high" >10% on detail screen, paired with conditional-scholarship warning), student_faculty_ratio (4.9-36.0), clinic/field/simulation seats + hands_on_per_student. DISPLAY-ONLY — nothing scored, eval unaffected. Loader validates; range test added.
- [ ] Optional later: career-fit nudge from hands_on_per_student for PI/government goals (eval-affecting — needs LLM-judge run)  → uses: reviewer (top)
- [x] College Scorecard JD debt + earnings (2026-06-10, commit 1eec8c8): API extract (CIP 22.01, cred level 7) → raw/scorecard_law_2201.json; build_scorecard.py matches 193/196 (3 unmatched have no Scorecard JD: jacksonville/wilmington/widener-commonwealth; ~32 matched-but-suppressed). scorecard_median_debt n=161 (median $106k, $45k-$204k), scorecard_earn_1yr/4yr n=159 (median $101k @4yr). Detail screen "real graduates" line + 2 compare rows. DISPLAY-ONLY — ranking unchanged (snapshot-verified).
- [ ] DECISION NEEDED: wire scorecard_median_debt/_earn into the financial model (replace estimated net_debt/salary or blend). This is the #1 judge ask BUT see memory eval-blindspot-cost-magnitude — the judge punishes accurate higher debt; expect a low win-rate that should NOT auto-revert. User call on model wiring + eval interpretation.  → uses: reviewer (top)
- [x] LSAT retake what-if (2026-06-10, commit 3ddb961): WhatIfPanel on results — +1/+3/+5 re-rank, lists full-tier upgrades (158 TX test: +3 → 26 upgrades). Hidden for no-LSAT profiles. NEEDS browser smoke-test (JSX is Babel-compiled in browser; API path verified via test_client).
- [x] Shareable profile link + CSV export (2026-06-10, commit 3ddb961): profile → URL hash on every match, restored+auto-run on open; copy button; Export CSV of filtered+sorted list (BOM for Sheets). NEEDS browser smoke-test.
- [x] CONFIRMED DEAD END (2026-06-10): placement-by-state is NOT in Employment_Summary_2025.xlsx (only state/local clerkships + foreign count) — per-school ABA PDFs remain the only source; Tier-3 blocker stands.

- [ ] MVP: try the feedback loop for real — after next vault triage session run `lists.py <list> --feedback --vault ...` and recalibrate grading/search buckets from the review-rates  → uses: none
- [ ] MVP: delete retired one-offs (_grade.py, _grade_h.py, _grade_j.py, _grade_n.py, export_horizons.py, export_jobs.py; finder.py kept for back-compat) once lists.py proven over a few cycles  → uses: none
- [ ] (TRIED & REVERTED) over-qualification dampener: composite multiplier (1-min((overshoot-3)/30, .45)) on schools far above the applicant's reach. Cleanly fixed p18 (Jacksonville off #1) but LLM-judge 7-11 (39%) -> REVERT. Regressions spanned high-stats non-cost-sensitive profiles (p07/p08/p12/p20), so it's not a calibration miss: the judge WANTS affordable safeties in lists, and any dampener strong enough to move Jacksonville off #1 also pushes down safeties it likes. Don't retry a global dampener; the Jacksonville-style intrusion is apparently tolerated by the judge. The matcher is now plateaued (data-blocked + judge rewards no further re-weighting).
- [ ] (still verify, low priority) 4 assigned elite ranks (Berkeley 10/Georgetown 14/UT 16/UNC 22) + soft estimated defaults (salary $90k/$58k, COL, lrap=moderate, target_states=[home]) are mine-from-memory/heuristic. Known minor data: wilmington (brand-new, no employment/bar outcomes -> honest zeros), ohio-northern (scholarship grid sums to 1.78 but no_scholarship_pct isn't scored, cosmetic), marquette bar=0 (WI diploma privilege).
- [ ] Optionally merge a real US News 2026 rank list for the 83-150 band (currently RNP-sentinel 999; prestige floors at ~0 past rank 84 so low impact).
- [ ] Refine plain web styling into final visual design (placeholder style in law/web/); render usnwr_rank_2026==999 as "Unranked" in web/server like the judge prompt now does.
- [ ] UI/UX streamlining pass (informed by stress test findings)
- [ ] p14 (no-LSAT/MA/65k-130k): UMass (in-state MA, affordable) now IS in the dataset, but expansion still regressed p14 — cheap out-of-state schools pushed MA schools down (the over-qualification/intrusion artifact above). Fixing the dampener should also fix p14.
- [ ] Add class_size_1l -> admissibility softener (transfers now done as a feature)
- [ ] Tier-3 data status: median_grad_debt now UNBLOCKED via College Scorecard (merged 2026-06-10, display-only; model wiring = open decision above). placement_by_state still blocked (per-school ABA PDFs only).
- [ ] Open PR for algo-optimization (pushed to origin; URL: github.com/lkchss/Opportunity/pull/new/algo-optimization)
- [ ] Render transfer-up plan styling in the final UI pass (TransferPanel is placeholder-styled)
- [ ] Add lower-ranked schools (ranks 83+) so the transfer-up launchpad list works for genuinely-not-T14 profiles (157/3.4 currently has 0 safety/target)
- [ ] Data enrichment HARD/predicted (later): selectivity trend (multi-year), NLJ250-by-market, part-time/transfer

### New feature requests (2026-06-04)
- [ ] Notable alumni/professors per school: scraper + UI DONE; BLOCKED on user OK to run live 196-school scrape, and on quality tuning (see In progress).  → uses: fact-checker (top), data-validator (subj)
      ⟳ skill-candidate: "wiki-enrich" — scrape Wikipedia school page, extract notable people, normalize into dataset field

### Feature requests — round 2 (2026-06-04) — ALL IMPLEMENTED, pending browser smoke-test + reviewer
- [x] Filter results by admissibility tier: tier chips (All / Safety / Target / Reach / Hard reach with live counts) above the results table; multi-toggle filters the listed schools. Empty-state when none match.
- [x] Cost-sensitivity help affordance: removed the note block; added reusable InfoTip ("?") component (hover/focus/click popover) next to the Cost sensitivity slider label.
- [x] Compare page stacked heroes: vertical stack of per-school radar + tier/composite/score chips above the side-by-side metric table.
- [x] Compare entry clarity: persistent "tick boxes to compare (2+)" hint in the compare-bar; CompareScreen shows a full empty-state CTA when <2 schools selected.
- [x] Remove "Profile" radar column from the results table.
- [x] Transfer-up interest checkbox on profile screen → when checked, results page shows School matches / Transfer-up tabs (TransferContent rendered inline). Teaser→separate-page kept only when unchecked.
- [x] Multi-state practice preference with weights (CA/NY=70/30): PracticeStates UI control (add/remove rows, weight inputs, live %); server _build_weighted_states; matcher _location_prefs + weight-averaged _compute_location_fit (extracted _single_state_fit). Backward-compatible with single target_state. +2 tests (46 pass). Eval unaffected (seeded profiles use single state → identical result).
      ⟳ skill-candidate: "weighted-pref-input" — reusable multi-select-with-weights control (states here, could generalize to other weighted prefs)

### Feature requests — round 3 (2026-06-04)
- [x] PracticeStates: default weight 100; adding a state resets all rows to an even split (50/50, 33/33/33…); removed the right-side % popup.
- [x] Tier filter is now single-select (All by default; clicking a tier shows only that tier).
- [x] LSAT/GPA number inputs are fully clearable (store raw string, no forced 0) so you can retype the whole number; server coerces on submit.
- [x] BUG FIX — admissibility score↔tier contradictions (162/3.5 splitter scored 76 but was "reach"; hard-reach at 50) + 4.0/180 showing every school "safety". Root cause: score (0.7·LSAT+0.3·GPA) computed independently of the both-median tier logic, and tiers ignored absolute selectivity. Fix: score banded to tier (_score_in_band) + acceptance-rate ceiling (_selectivity_ceiling). 0 contradictions across 5×196 checks; 46 tests pass. LLM-judge eval RAN: candidate 15-8, 65% win-rate → KEEP (losses spread, not concentrated).  → uses: reviewer (top)
- [x] Transfer-up page → two equal side-by-side panels (launchpads | targets), arrow removed (user picked side-by-side over results-list styling).
- [x] Reviewer pass (2026-06-04) on full session diff — 3 findings, all fixed + retested (47 pass): (1) no-LSAT score broke its own tier band (0.85 applied AFTER banding → "target" could show 53); moved penalty before banding. (2) int(lsat) outside try → 500 on bad API input; wrapped _build_profile, now 400. (3) NaN GPA bypassed guards; added isnan check. Verdict was fix-before-merge → now resolved.

### Feature requests — round 4 (2026-06-05)
- [x] UI batch: explainer modal ("How it works") + "Report bug / feature" modal (mailto) in topbar; removed title subtitle; "I haven't taken LSAT" moved under LSAT box (smaller); in-state tuition now an add-row state picker (StateList) like practice-state; practice-state moved to bottom of left col; removed slider hint text (kept cost "?" tooltip); tier filter is now a right-aligned filter icon → dropdown (replaces chip bar); results subtitle trimmed to "click any row for detail"; list-length selector (10/20/50/All) at top + removed bottom "Show all"; stacked compare heroes → single overlaid RadarOverlay + color legend; back buttons left-aligned (back-link); "Composite" → "Score" everywhere; removed transfer teaser + standalone transfer page (transfer is tab-only when opted in at landing).
- [x] ABA living expenses (2026-06-05): merged real per-school ABA living budgets into law_schools.json (living_expense_annual, all 196); _compute_financial uses them (fallback to COL heuristic). UCLA in-state debt verified accurate. 47 tests pass. Isolated eval → 19%/REVERT but confirmed ARTIFACT (real costs ~$11k/yr higher at 78/100; judge rewards lowballed debt). USER DECISION: KEEP accurate data (override eval). See memory eval-blindspot-cost-magnitude.

### Feature requests — round 5 (2026-06-05)
- [x] Transfer-up plan is now profile-aware: launchpad score = mobility 0.40 + location 0.30 + career 0.20 + in-state 0.10; targets bounded to a realistic jump window (rank floor = 0.40×best_realistic) and scored openness 0.45 + location 0.35 + career 0.20. Verified TX/NY/CA give different launchpads+targets. +match_score field. test_launchpads_sorted_by_out_rate → match_score + new profile-responsiveness test. 48 pass.
- [x] Filter tiers multi-select (dropdown checkboxes; All = all four).
- [x] Results subtitle "click on a school for more details"; topbar buttons lowercased ("how it works", "report bug / request feature").
- [x] Results career columns are now goal-driven (buildResultCols/GOAL_CAREER_COLS): e.g. Public Interest shows Pub.Int% + Gov% instead of always BigLaw%/Clerk%.
- [x] Weight career paths (multi-goal): CareerGoals weighted picker (default BigLaw 100, even-split on add) → goals_weighted; matcher _career_goals + _single_goal_component blend; server _build_weighted_goals + derives primary goal for salary/columns. Results columns reflect selected paths (one goal → 2 metrics, several → each goal's primary, capped 3). +2 tests (50 pass). Eval: diff vs pre-change snapshot = 0 ranking changes (backward-compatible no-op on single-goal eval suite), so no re-judging needed. Verified weighting reorders (BigLaw→WashU/BC, PI→CUNY/Northeastern).
- [ ] Practice-area placement granularity (IP/litigation/corporate %): NOT in ABA data (employment is by type + firm size only). Existing practice_area_strengths tags are NOT legitimately sourced — build_schools.py sets [], and the 75 tagged schools are the hand-authored original seed (no citation, likely memory/LLM-generated) → DO NOT surface in UI. Future: source real per-practice placement (NLJ250-by-practice, LST, or school employment-by-industry surveys) and/or replace the unsourced tags with cited data.  → uses: data-validator (subj), fact-checker (top)

### Feature requests — round 6 (2026-06-05)
- [x] Polish batch: report modal emojis removed + button "Send"; goal weighting removed (CareerGoals = plain multi-select, equal weight); removed in-state/practice empty hints; state pickers start with one empty row; filter starts unchecked (none=all, no "All tiers" option); removed compare tooltip + "pick at least 2"; tabs → Matches/Transfers; removed text above compare tabs; rank 999 → "N/A" via fmtRank() everywhere; server filters blank in-state rows.
- [x] REDESIGN transfer pipeline (user chose tier-relax + gate-on-takes-transfers): transfer_up_plan now ranks via the full matcher (rank_schools internally). Launchpads = safety/target tier by composite (profile-aware). Targets = "reach" tier (the realistic upgrade, no faked stats) GATED on transfers_in>0, ranked composite 0.70 + openness 0.30 (fit-dominant per "takes transfers, then fit"). UI/tests updated (50 pass). Launchpads strongly profile-vary; targets vary where the reach pool has location-relevant transfer-takers (narrow for low-stat applicants). Not in rank_schools → no eval impact.

## In progress
- [~] Finances clarity (started 2026-06-04): UI relabel done (slider "Scholarship priority"→"Cost sensitivity" w/ hint; results col "Net debt"→"Cost after aid", still sortable; note rewritten). OPEN product question: should "cost sensitivity" change matcher MECHANICS (drive financial weight directly) or stay label-only over the existing scholarship-slider→cost_sensitivity floor? Re-weighting is LLM-judge-punished (matcher plateaued), so deferred pending user call.  → uses: reviewer (top)

## Done
- [x] MVP loop consolidation (2026-06-09): mvp/lists.py = one CLI for all 4 lists (opps/horizons/jobs/network), replaces _grade*.py + export_{horizons,jobs,network}.py. Output levers: grade-at-ingest (fit_grade/deadline/status in search JSON, no title-match dicts), URL-normalized dedupe (utm/case/slash), search_log w/ --bucket → --stats yield-per-bucket, --titles pre-search context dump, --stale passed-deadline report (found 20 in opps, 5 at A-), --feedback reads vault seen/review triage back into DB + review-rate by grade/category. 70 results_batch*.json archived → mvp/ingested/. CSVs regenerated +Status col. 10 new tests (77 total pass).
      ⟳ skill-candidate: "opportunity-cycle" — search (w/ --titles context) → ingest --bucket → csv/obsidian export → periodic --feedback + --stale
- [x] Notable alumni/professors (2026-06-04→05): build_notable.py scraped all 196 schools (initial 166 + 30 rate-limited tail re-filled at delay 2.5), --merged into law_schools.json. Coverage: 143 schools with ≥1 person (880 alumni + 597 faculty); 88 with faculty. Faculty high-quality (spot-checked Harvard/Yale/Stanford/Georgetown/NYU/Columbia — real professors). Alumni empty for many elite schools (their alumni live on separate "List of … alumni" pages we skip — coverage limit, not bad data). Fixed citation-link bug (Wayback Machine etc. captured as people) via _SKIP_EXACT denylist + scrubbed cache (6 junk removed). DetailScreen renders "Notable people" block w/ Wikipedia links; verified in API. 64 tests pass.
- [x] Side-by-side school comparison (2026-06-04): row checkboxes + compare bar in ResultsScreen, CompareScreen with "With your stats" (fit scores/net cost) vs "Raw school stats" toggle, best-in-row highlight. Backend unaffected (verified /api/match). reviewer not yet run.
- [x] Interactive transfer-up page (2026-06-04): TransferScreen — launchpads → targets pipeline, click-through to school detail, empty-state handling; TransferPanel on results became a teaser linking to it. Built on existing transfer_up_plan.
- [x] Admissibility clarity (2026-06-04): DetailScreen now states the bar is a 0–100 fit score, NOT odds of admission; tier is the admit read. Bar relabeled "Admissibility fit".

## Done
- [x] Repair + wire test suite: tests referenced the pre-restructure `app/` package. Migrated `test_data_loader.py` to `law.data_loader` and updated the `VALID_SCHOOL` fixture with the 10 schema fields it had drifted from (state, usnwr_rank_2026, is_public, solo_small_firm_pct, jd_required_pct, median_{private,public}_sector_salary, target_states, annual_tuition_{resident,nonresident}); deleted obsolete `test_matcher.py` (tested a removed scoring API, superseded by test_law_matcher.py). Suite now 61 passing. Added a PostToolUse hook (`.claude/hooks/run-tests.ps1`) that auto-runs the suite after any in-project `.py` edit, silent on pass, surfacing failures.
- [x] Verify the 121 new schools + fix tuition bug: audit found 27 schools with annual_tuition=$0 (they bill per-credit; *Annual cols blank). Fixed build_schools to fall back to FT_*_Credit x 30 credits/yr. Now all 196 have valid tuition, unique ids, valid states, monotonic LSAT/GPA percentiles, real career data. Bonus: correct tuition knocked the p18 Jacksonville intrusion from #1 to #4.
- [x] Expand coverage 75 -> 196 schools: add all 121 missing ABA schools via law/data/build_schools.py (base record from First_Year_Class admissibility + Tuitions + The_Basics; enrichment fills real career/scholarship/bar/transfers). Fixes the coverage cliff (was 0 schools with median LSAT <=155; sub-156 applicants now get real safety/target lists). Includes elite gaps Berkeley/Georgetown/UT-Austin/UNC + UMass. LLM-judge 12-9 (57%) -> KEEP (marginal; depressed by the over-qualification artifact now queued as next lever). Also: dti computed from rounded net_debt (test invariant); judge prompt renders RNP.
- [x] Remove unused LLM narrative generator (law/llm_client.py + dead app-import test). Not wired into server/matcher.
- [x] Fix p03 regression: cost-sensitivity financial-weight floor in _composite (scholarship slider no longer starves financial weight). LLM-judge 10-5 (67%) -> KEEP. Fixed p03/p09/p16/p23; losses spread (p01/p02/p08/p15/p17), not concentrated.
- [x] Push algo-optimization branch to origin (17 commits).
- [x] Conditional-scholarship penalty scaled by real reduction_rate (data + matcher). LLM-judge 5-4 (56%, marginal) -> KEEP.
- [x] Wire bar_pass_vs_state into career fit (±10% value-add). LLM-judge 17-7 (71%) -> KEEP, commit 35e6f49.
- [x] Add ABA 2026 bar passage data (first-time, state avg, vs-state, ultimate) for all 75. Commit + detail-screen surfacing.
- [x] Finish transfer feature: surface mobility + bar passage on school detail screen.
- [x] Transfer-up feature: _transfer_metrics + transfer_up_plan (launchpads/targets) in matcher, /api/match transfer_plan, TransferPanel UI, 6 tests. Commit daa4697.
- [x] Tier-3 available part: recompute 5 real career outcome rates from ABA Employment Summary. LLM-judge 17-7 (71%) -> KEEP, commit 5adb764.
- [x] Normalize grid scholarship generosity to dataset max. LLM-judge 13-2 (87%) over iter-1 -> KEEP, committed d9f90f3.
      Recovered 5 of 6 prior regressions; regressions now 2 (p03, p14).
- [x] Wire ABA enrichment into matcher (real aid grid, grid generosity + conditional penalty, real placement net of school-funded).
      LLM-judge: 18-6 (75% win) over baseline -> KEEP, committed 971bb9c. Real data beats re-weighting (earlier weighting iters ~40%).
- [x] Data enrichment EASY tier: all 75 schools populated from ABA 2025 bulk reports via law/data/build_enrichment.py
      (class_size_1l, scholarship grid, conditional_scholarship, employed_10mo_pct, school_funded_pct) + transfers_in/out
- [x] Build LLM-judge optimization harness (eval/): seeded profiles, blind pairwise diff-only judging via zero-context agents, win-rate gate
- [x] Commit current working tree (all law/ changes since session start) — baseline commit 64f859a on branch algo-optimization
- [x] Build Flask web app (law/server.py + law/web/) from the mock-up: Profile form -> Results table -> School detail, wired to rank_schools (plain placeholder styling)
- [x] Restructure repo into two product lines: mvp/ and law/
- [x] Design and implement five-score matching algorithm (admissibility, career fit, location fit, scholarship, financial)
- [x] Add PSLF/IDR path as display-only financial breakdown (not part of score)
- [x] Build Streamlit UI with new form inputs (target state, in-state residency, income bracket, scholarship slider)
- [x] Verify and patch all 22 schools in law/data/law_schools.json with ABA 509 data (7sage.com 2025-26 cycle)
- [x] Add ranks 42-49 (10 schools) — 32 schools total
- [x] Add ranks 52-70 (18 schools) — 50 schools total
- [x] Write 37-test stress suite covering edge cases, score sanity, ranking stability, financial math (37/37 passing)
- [x] Add T14 (15 schools, ranks 1-13) + ranks 70-82 (10 schools) — 75 schools total (ranks 1-82)
